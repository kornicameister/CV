---
interface SkillCategory {
  category: string;
  subskill: string[];
}

interface Props {
  skills: SkillCategory[];
}

const { skills } = Astro.props;

// Map LaTeX FontAwesome codes to FontAwesome classes
const iconMap: Record<string, string> = {
  '\\faAws': 'fab fa-aws',
  'AI': 'fas fa-brain',
  'Backend': 'fas fa-server',
  'Containers': 'fab fa-docker',
  'Databases': 'fas fa-database',
  'GIT': 'fab fa-git-alt',
  'CICD': 'fas fa-infinity',
  'General': 'fas fa-star',
  'Other': 'fas fa-ellipsis-h',
};

// Helper function to get icon class
function getIconClass(category: string): string {
  return iconMap[category] || 'fas fa-code';
}

// Helper function to get display name
function getDisplayName(category: string): string {
  // Remove LaTeX command prefix if present
  if (category.startsWith('\\fa')) {
    return category.substring(3).toUpperCase();
  }
  return category;
}
---

<section id="skills" class="section">
  <div class="container">
    <h2 class="section-title">Skills</h2>
    <div class="skills-grid">
      {skills.map(category => (
        <div class="skill-category">
          <h3 class="category-name">
            <i class={`category-icon ${getIconClass(category.category)}`}></i>
            <span>{getDisplayName(category.category)}</span>
          </h3>
          <div class="skill-pills">
            {category.subskill.map(skill => (
              <span class="skill-pill">{skill}</span>
            ))}
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .section {
    padding: 4rem 0;
    scroll-margin-top: 2rem;
    background: var(--bg-secondary);
  }

  .section-title {
    font-size: 3rem;
    font-weight: 800;
    text-align: center;
    margin-bottom: 3rem;
    background: var(--accent-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .skills-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .skill-category {
    background: var(--bg-card);
    border: 1px solid var(--border-color);
    border-radius: 12px;
    padding: 1.5rem;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .skill-category:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 24px var(--shadow);
  }

  .category-name {
    font-size: 1.3rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: 1rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .category-icon {
    font-size: 1.5rem;
  }

  .skill-pills {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .skill-pill {
    display: inline-block;
    background: var(--bg-secondary);
    color: var(--text-secondary);
    padding: 0.4rem 0.8rem;
    border-radius: 20px;
    font-size: 0.9rem;
    border: 1px solid var(--border-color);
    transition: background 0.3s ease, color 0.3s ease, border-color 0.3s ease;
  }

  .skill-pill:hover {
    background: var(--accent-gradient);
    color: white;
    border-color: transparent;
  }

  @media (max-width: 640px) {
    .section {
      padding: 3rem 0;
    }

    .section-title {
      font-size: 2rem;
    }

    .skills-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }
  }

  @media (min-width: 641px) and (max-width: 1024px) {
    .skills-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
</style>
